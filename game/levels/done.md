---
id: done
title: That's the tour
role: summary
---

You have now seen every item type and feature this engine offers: chapters grouping the sections, prose pages with each `role` (including the `how-holes-work` page behind the header link), the three puzzle roles (`core`, `pretest`, `extra`), hints with a `when-goal`, prerequisites, remedies, a structured inventory, a gated check with a `forbidden` move, and a multi-block prelude.

To make this game your own, edit `game/game.yaml` and the files under `game/levels/`. The engine's `docs/authoring.md` is the full reference.
